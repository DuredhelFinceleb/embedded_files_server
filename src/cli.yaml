name: embedded_files_server
version: "0.1.0"
about: Embeds files into an exe an serves them as HTTP server
args:
    - port:
        long: port
        value_name: PORT
        about: Port to bind webserver to
        takes_value: true
        default_value: "3000"